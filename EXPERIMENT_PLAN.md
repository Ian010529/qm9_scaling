# QM9 Scaling Law：完整实验方案

版本：v1.0（方案完善版）  
日期：2026-09-07  
研究分支：`research/scaling-law-v1`  
基于代码版本：`fdd0a611e486f25e5c4ccd8c148db9f5a6b782d2`

> 本次交付是实验方案，不是训练代码实现。保留发现 scaling law 的研究目标，保留现有三条主路线、四个性质、八个训练规模及五次重复，在其上补齐实验、分析、对照与投稿流程。下文的新增功能和运行记录是执行要求，不表示仓库已经实现或运行了它们。本文件不修改 `config/experiment.json` 或任何现有数据。

## 1. 研究目标与最终产出

核心目标：发现并验证 QM9 分子性质预测中，预测误差随训练数据量变化的 scaling law，研究规律如何受到分子表示、预测目标、模型容量和预训练表示的影响，并检验规律是否具有预测更大训练规模表现的能力。

完整研究回答五个问题：

1. 三条路线、四个性质的误差是否存在稳定的幂律或带平台项的幂律关系？指数、平台项和适用区间分别是什么？
2. 用小规模训练结果能否预测 50k、100k 规模的误差，而不只是对已经看到的曲线进行拟合？
3. 路线间是否存在随数据规模变化的优势转换？相同目标误差分别需要多少数据？
4. 观察到的平台、指数差异和转换点，分别与表示信息、目标组成效应、模型容量和优化充分性有什么关系？
5. 在结构分组划分及新的随机划分下，这些现象是否保持？

最终产出包括：完整逐次运行记录、主学习曲线、候选规律比较、预先保存的外推预测、外推验证、机制对照、稳健性结果、可复现附件及论文初稿。发现规律是研究目标，不预先假定所有组合都必然服从同一规律；不支持单一幂律、存在性质依赖或存在分段规律，同样保留在论文中。

相关工作已研究分子表示中的数据规模、模态、预训练、划分和模型容量[2]。因此不将“首次画出分子预测学习曲线”作为创新点。论文重点放在可预测性验证、目标依赖、表示瓶颈及机制对照形成的完整证据链。

## 2. 保留的主实验

### 2.1 数据和目标

沿用 `qm9_reference_smiles_v2`：原始 133,885 条，删除官方异常名单 3,054 条，按当前无立体标记规范化 SMILES 去重 87 条，最终 130,744 条。保留现有分子 ID、排序、排除记录和数据哈希[1]。

| 目标 | 定义 | 原单位 | 主分析 |
|---|---|---|---|
| mu | 分子偶极矩的标量大小 | Debye | 原单位 MAE；RMSE 为补充 |
| homo | HOMO 能量 | Hartree | 原单位 MAE；RMSE 为补充 |
| lumo | LUMO 能量 | Hartree | 原单位 MAE；RMSE 为补充 |
| G | g298，298.15 K 下分子总 Gibbs 自由能 | Hartree | 直接预测为主；组成与残差实验解释结果 |

三个能量目标不在数据文件中改单位。论文可另列 eV 换算展示，但原单位结果、转换常数及其来源必须同时保存。不同目标分别训练，不把多任务训练混入主实验。

所有主路线继续使用当前二维结构，不引入三维坐标。保留 `smiles_isomeric` 用于追踪；当前去重协议是按相同二维输入保留最小原始编号，不声称被合并记录在所有化学意义上完全等价。补充记录去重组的数量、组大小和标签离散程度；涉及测试标签的分析在最终解封后进行，不据此重新筛选数据。

### 2.2 路线与输入

| 路线 | 固定输入 | 学习部分 |
|---|---|---|
| Morgan-binary + XGBoost | radius=2、2048 位、无手性位 | XGBoost |
| D-MPNN | Chemprop 2.2.1 对应的分子图，72 维原子、14 维有向边特征 | 消息传递和回归头端到端训练 |
| Frozen MoLFormer + MLP | 当前固定模型提交产生的 768 维向量 | 仅训练 MLP |

MoLFormer 继续使用 `ibm-research/MoLFormer-XL-both-10pct`，revision 为 `7b12d946c181a37f6012b9dc3b002275de070314`，`pooler_output`、eval、float32、冻结参数。该具体版本使用 10% ZINC 和 10% PubChem 进行预训练[3]，不把它写成完整 1.1B 数据版本。外部预训练与 QM9 的结构重叠若无法获得可核验数据，标注“未完成重叠审计”，不写“已排除”。

图缓存是输入，不是已经训练的 D-MPNN 表示。冻结向量允许预先全量提取，但下游输入标准化、目标标准化、组成回归和所有可学习变换只拟合当前训练子集。

### 2.3 规模、划分和重复

固定划分仍为训练池 104,595、验证集 13,075、测试集 13,074；划分种子 `20260905`。

训练规模保持：`500, 1000, 2000, 5000, 10000, 20000, 50000, 100000`。

主重复保持：`(subset_seed, training_seed) = (11,101), (22,102), (33,103), (44,104), (55,105)`。每条序列取前 N 个 ID，保持规模嵌套。所有路线和对照在相同重复编号上使用相同分子集合。

主实验规模不变：`3 × 4 × 8 × 5 = 480` 次训练。现有 1k/HOMO XGBoost 结果是 pilot，保留但不替代按正式协议执行的主结果。

这里的 N 表示训练样本量，不表示整个研究只访问了 N 个标签。固定验证集以及下述调参使用的标签另行报告。较大 N 的子集高度重叠，五次重复不是五个独立化学总体；主结论首先条件于当前有限训练池和固定评估集。

## 3. 调参与训练协议

### 3.1 固定配置曲线与调优曲线分别回答什么

主曲线使用每个“路线 × 目标”一套确定的超参数，并在全部八个 N 上保持不变。这样首先研究一个确定学习系统增加数据后的规律。

另外在 `500、5000、50000` 三个规模做预算一致的重新调参诊断，检查主配置是否系统性偏向某一数据规模。诊断结果独立报告，不能逐点替换主曲线中较差的结果。由于该诊断包含 50k，安排在外推预测文件保存之后。

### 3.2 主配置选择

只使用训练池与验证集，测试集不参与任何模型、特征或训练超参数选择。

建立专用调参序列：训练池按 subset seed `9173` 排列，使用 `1000、5000、20000` 三个嵌套规模；训练种子为 `991、992`。它与主训练池可以重叠，不能声称这是独立的外部调参数据，也不能声称主小样本实验完全不受额外标签的帮助。

每个“路线 × 目标”评估八组配置：下表的一个起始配置，加上用 NumPy PCG64 随机种子 `7347` 从对应离散空间无放回抽取的七组配置。按表中字段顺序构造笛卡尔积，排序后抽样；保存完整八组候选，执行时不重新抽样。各目标共享同一路线的候选表。

第一轮八组候选均在三个调参规模、种子 991 上训练。对每个目标，评分为三个规模上 `log(验证MAE / 相应训练中位数基线验证MAE)` 的平均值；分母为零时使用 `max(分母, 1e-12)` 并记录。该分母对同一规模的所有候选相同，只用于平衡不同规模。

前两名在三个规模上用种子 992 再运行，按两种子、三规模的平均评分选择最终配置。数值差不超过 `1e-4` 时优先选择较少参数/较浅树，再按候选编号破同分。保存所有候选结果，不只保存胜出配置。

三条主路线调参工作量为 `3×4×(8×3 + 2×3)=360` 次候选训练，不计预算自动延长。它不计入 480 次主实验。

### 3.3 候选空间与起始配置

以下参数是本研究预先采用的操作性设计，不宣称是文献证明的普适最优配置。

| 路线 | 候选空间 | 起始配置 |
|---|---|---|
| XGBoost | max_depth={4,6,8}; learning_rate={0.01,0.03,0.1}; min_child_weight={1,5,10}; subsample={0.8,1.0}; colsample_bytree={0.8,1.0}; reg_lambda={1,10,100} | 6,0.03,1,0.8,0.8,1 |
| D-MPNN | hidden_dim={300,600,900}; depth={3,5,7}; ffn_layers={1,2}; dropout={0,0.1,0.2}; lr={0.0001,0.0003,0.001}; weight_decay={0,0.00001,0.0001} | 300,3,1,0,0.001,0.00001 |
| MoLFormer + MLP | hidden_dim={256,512,1024}; hidden_layers={1,2,3}; dropout={0,0.1,0.2}; lr={0.0001,0.0003,0.001}; weight_decay={0,0.00001,0.0001} | 512,2,0.1,0.001,0.00001 |

XGBoost 使用 `hist`、平方误差训练目标、MAE 选模，未列出的正则项显式保存。D-MPNN 使用官方 BondMessagePassing；FFN 宽度等于消息宽度；mu/homo/lumo 使用 mean 聚合，G 使用 sum 聚合，均显式记录。神经网络使用 ReLU、AdamW、有效 batch size 128，不启用 batch normalization。MLP 的每个隐藏层宽度相同。

### 3.4 训练充分性与早停

神经网络：训练目标为标准化目标的 MSE；验证选模指标为还原原单位的 MAE。目标均值/标准差用当前训练子集计算；MLP 输入同样只使用当前训练子集拟合逐维均值/标准差，近零标准差维度以 1 代替。

采用有效 batch size 128，不丢弃最后一个 batch；每个 epoch 重新洗牌并记录种子。初始学习率从选定学习率的 0.1 倍在前 1,000 次更新线性升到选定值。之后使用验证 MAE 驱动的 ReduceLROnPlateau：连续三次评估相对改善不足 `1e-4` 时学习率乘 0.5，下限 `1e-6`。每 500 次更新及训练结束评估；至少训练 5,000 次更新后才允许早停；连续十次评估无上述改善则停止。初始最大更新数 60,000，梯度范数裁剪为 5。训练期间和最终评价使用相同反标准化逻辑。

XGBoost：初始最大树数 10,000，early stopping patience=300，使用验证 MAE 选择最佳迭代；不把最后一轮默认当作最佳模型。

若最佳迭代位于预算最后 10%，且最后 20% 预算内最佳验证 MAE 相对改善超过 0.1%，按预定规则把总预算扩到 2 倍，必要时再到 4 倍。神经网络从完整 checkpoint 续训，XGBoost 在相同种子和配置下重跑更大上限，避免改变续训语义。达到 4 倍后仍满足该条件，记录 `optimization_censored=true`，保留结果并在平台解释中考虑优化不足；不把这种点直接解释为表示的不可约误差。

以上延长规则对候选和主运行一视同仁，由验证结果自动决定，不使用测试指标。所有曲线同时展示是否触及训练上限。

### 3.5 随机性、保存与恢复

每次运行从头初始化，不从较小 N 的模型 warm start。记录 Python/NumPy/PyTorch/XGBoost 的种子、设备、精度和确定性设置。GPU 无法满足确定性设置时明确记录，不默默把不同精度混成同一重复。

保存最佳模型及完整训练恢复状态：模型、优化器、调度器、随机数状态、sampler 状态、当前更新数和最佳指标。基础设施中断续跑同一 run；数值失败记录后按同配置同种子复跑一次。仍失败则保留失败标记，不换种子直到跑出较好结果。

运行目录应由 dataset/split/route/target/N/subset_seed/training_seed/config_hash 唯一确定。只有全部产物完成校验后才写入完成标记；恢复调度时跳过已完成运行，不覆盖结果。

## 4. Scaling law 分析：先拟合，再进行真正的规模外推

### 4.1 分析量与候选规律

每条路线、每个性质单独建模。主响应变量是原单位测试 MAE，RMSE 做完整平行分析；不把四个性质直接混合成一个物理意义不明的误差。对数拟合使用 `log(E)`，数值下限 `1e-12`；用 `x=N/1000` 改善拟合条件。

预先比较以下候选，不只比较“幂律拟合的 R² 是否很高”：

- M0：常数，`E(N)=c`。
- M1：无平台幂律，`E(N)=A*x^(-alpha)`。
- M2：带平台幂律，`E(N)=E_inf+A*x^(-alpha)`。
- M3：非幂律对照，`E(N)=E_inf+A*exp(-k*x)`。
- M4：连续分段幂律，`E=A*(N/Nc)^(-alpha1)`（N≤Nc），否则 `E=A*(N/Nc)^(-alpha2)`。Nc 从 `{1000,2000,5000}` 中选择；每一侧至少有两个训练规模。M4 不再加自由平台项，避免同时堆叠过多参数。

约束 `A>0, c>0, E_inf>=0, alpha/alpha1/alpha2>=0, k>0`。主拟合指数上界为 3；碰到边界时再按预定敏感性分析扩大到 5，分别报告，不能隐藏边界结果。平台项不超过拟合区间内最小平均误差。幂律指数初值用 `{0.1,0.3,0.5,1,2}`；平台初值用最小平均误差的 `{0,0.25,0.5,0.75,0.9}` 倍；使用全部适用初值保留最小损失解。M3 的 k 初值为 `{0.001,0.01,0.1,1}`。

拟合对象为五次重复的平均误差曲线，主损失是各 log(N) 规模点等权的 log-error 平方损失。记录残差、优化器状态、边界触及情况及有效参数数；不能把同一嵌套序列上的八个点当成八次独立试验。

### 4.2 双层验证，避免“看完大规模结果再解释”

**开发区间：500–20k，共六个规模。** 在小规模开发结果上做 rolling-origin 选择：用前四个规模预测 10k，再用前五个规模预测 20k；M4 在每次训练时仅枚举满足两侧数量要求的 Nc。各候选按两个留出规模的绝对 log 相对误差平均排序；差异小于最佳评分的 1% 时优先参数更少的模型，再按 M0–M4 编号破同分。

**确认区间：50k、100k。** 用开发区间六点拟合已选规则，先保存每组的候选比较、最终模型、参数和 50k/100k 预测及区间，再揭示对应大规模评估结果。

执行顺序是：完成基础 HPO → 主模型训练 → 只发布开发区间测试结果供规律选择 → 保存外推预测 → 再发布 50k/100k 的验证汇总及测试指标。大规模训练的验证值允许训练程序用于既定早停，但在外推文件保存前，不向负责规律选择的分析流程提供这些日志和汇总。不是依靠“研究者尽量不看”，而是在执行时分开结果目录与分析输入。

一旦使用开发区间测试结果，它就承担了规律开发数据的角色，而不再是所有研究决策都未接触过的最终盲测数据。确认检验的独立性来自预先留出的训练规模；它仍与开发曲线共享固定测试分子。论文准确称为“held-out training-size extrapolation”，不称为全新分子总体上的独立外部验证。

不能用 50k/100k 结果重新挑选最有利的候选规律，再把它称为盲测成功。可以在确认后补做全八点描述性拟合，但须另表列出，与事先预测并排展示。

### 4.3 外推判据与规律分级

主外推误差为 `abs(E_pred-E_observed)/E_observed`，并报告绝对 log 比值；每个确认规模单独列出，不只给平均数。同时比较把 20k 误差保持不变的简单预测。

本项目预先采用的实用参考线：两个确认规模的相对误差均不超过 10%，且平均相对误差比“保持 20k 不变”至少减少 20%。这两个阈值是研究设计中的实用判据，不是通用统计定理；同时公开所有连续误差，不因刚好越线就丢弃结果。

报告三个层次：

1. **具有规模外推证据的规律**：开发选择、确认表现、重复稳定性及诊断相互支持。
2. **仅描述性拟合有效**：区间内拟合较好，但外推不稳定或参数无法辨识。
3. **当前范围不支持所设规律**：常数/非幂律对照更合适、分段差异显著，或训练尚不足以作出判断。

不要求十二组全部呈现同一 alpha 才称研究成功；也不因为某条曲线不符合预想就删去目标或缩小原始范围。

### 4.4 不确定性与参数可辨识性

主报告展示五条原始重复曲线、均值和标准差。对曲线参数和外推预测做 2,000 次整条重复曲线 bootstrap，随机种子 `20260907`；一个重复被抽中时保留其全部 N。比较模型时对同一重复编号使用同一抽样，保留配对关系。

这些区间条件于当前训练池与固定分割，不把它们称为化学总体的精确置信区间。另做逐一删除一个重复的五次敏感性分析。五次重复不足以支持非常精细的尾部概率主张，应同时报告原始分布。

对测试分子不确定性，另外使用共同分子 ID 权重进行 1,000 次 bootstrap：每次对同一 split 的所有路线、N、重复使用同一组分子抽样。报告“训练重复变动”和“测试样本变动”的不同区间，不把二者直接相加。结构分组实验以结构组作为测试重采样单位。

bootstrap 时重新执行候选选择，报告各候选被选中的比例；同时列出固定候选下的参数区间，以区分模型选择不确定性和参数不确定性。

对 E_inf 和 alpha 记录联合变化、边界比例、不同初值的解差异及留一重复敏感性。如果平台区间很宽或高度依赖初值，写“平台尚不可辨识”，不把拟合常数当作已经测得的物理误差下限。M4 的 Nc 是离散候选，报告选择频数；不能据此声称精确找到了某一个样本数的相变。

十二个路线×目标组合及两种误差指标全部展示。默认区间是逐组区间，不用它们声称十二组同时显著，也不事后选择最显著的少数组合代替完整结果。

### 4.5 数据效率、交叉点与平台解释

局部指数为相邻规模的 `-[log(E2)-log(E1)]/[log(N2)-log(N1)]`，与全局 alpha 并列展示，不将局部波动直接称为新规律。

两路线预测误差相等的位置定义为交叉点。只报告 500–100k 内有支持的交叉；没有交叉或多数 bootstrap 无交叉时如实记录，不强行给一个数。

数据效率以固定阈值展示：对每个目标，以三条主路线在 500 样本上的平均 MAE 为 E_ref，预先报告 `0.75、0.5、0.25 × E_ref` 三个水平所需样本数。先使用观测范围内 log-linear 插值；再单列规律预测。未达到阈值的路线写 `>100k`，不能把远超数据范围的估算当作已验证结论。

## 5. 预先安排的解释性对照

这些对照是对主研究的完善，不替换或删除主路线，也不是看完结果后才决定是否补做。原始 G 和 binary Morgan 结果始终保留。

### 5.1 组成基线和 G 残差

所有 N、所有重复保存训练均值和训练中位数预测基线。另提取 H/C/N/O/F 原子数，其中 H 含隐式氢。

对 G 仅用当前训练子集拟合带截距的最小二乘组成模型：`G_comp=b+sum_z(w_z*n_z)`，采用 SVD 最小二乘并保存秩、奇异值和 rcond=1e-12。它是统计组成模型，不把系数解释成严格孤立原子自由能。

残差训练定义为 `deltaG=G-G_comp`，预测还原为 `G_hat=G_comp+deltaG_hat`。三条路线使用同一个对应 N/重复的组成基线。残差的目标标准化只拟合训练残差，最终评价仍在原单位 G 上进行。直接 G 与残差 G 使用相同架构和训练规则；残差实验允许相同预算的独立目标调参，并完整计入成本。

追加 `3×1×8×5=120` 次残差训练。比较组成基线、直接预测和残差学习的误差曲线及指数，回答总能量趋势与结构相关剩余信息的关系。

### 5.2 表示与预测头对照

新增三条辅助路线，覆盖四目标、八规模、五重复，每条 160 次，共 480 次：

- Morgan-count + XGBoost：radius=2、2048 维、无手性，保留次数；同时保留 binary 结果。
- Morgan-binary + MLP：使用主 MLP 候选空间，按第 3 节独立调参。
- Frozen MoLFormer + Ridge：输入只在训练子集标准化，拟合带截距 Ridge；alpha 候选 `{0.0001,0.001,0.01,0.1,1,10,100,1000,10000}`，在三个调参规模按相同评分选定一个 alpha/目标，并在全部 N 固定。Ridge 不使用随机初始化，五次重复对应不同训练子集。

count-XGBoost 首先复用 binary-XGBoost 的配置，形成单一表示因素改变的配对对照；另外在第 3.1 节诊断规模采用相同预算调参，以判断配置迁移是否不利。不要把“更换表示同时重新调参”的差异全归因于表示本身。

上述比较能够解释完整预测路线的差异，但仍不单独隔离预训练的因果效果。没有相同编码器的有/无预训练对照时，措辞为“冻结预训练路线的下游数据效率”，不直接归因于 Transformer 架构本身。

统计相同输入向量组的组数、涉及分子数、组大小和组内标签离散程度。由组内中位数计算的最小可达经验 MAE 只在最终解封后作为表示诊断，不作为可部署预测器或总体不可约误差估计。不要通过删除相同指纹的不同分子来偏向某条路线。

### 5.3 容量与优化对照

在 `500、5000、50000` 三个规模、四目标、五重复，对三条主路线各增加一个容量档：XGBoost max_depth 增加 2；D-MPNN 消息和 FFN 宽度同时乘 2；MoLFormer 下游 MLP 隐藏宽度乘 2。其他参数保持对应主配置，按相同早停和自动延长规则训练。追加 `3×4×3×5=180` 次。

结合主训练的自动预算延长记录，区分容量限制和优化未完成。第 3.1 节的独立调参诊断用于检查仅增加容量但未匹配学习率的风险；不把大模型一次表现变差直接解释为容量无帮助。只得到训练误差平台而未完成这些检查时，不给出“物理信息极限”的结论。

### 5.4 抽样与初始化方差

在 `1000、20000` 两个规模，对三条路线和四目标使用 `3 subsets {11,22,33} × 3 training seeds {101,102,103}` 的交叉设计，共 216 个单元。其中与主实验相同配置的 72 个对角单元复用已完成结果，额外训练 144 次。

通过两因素随机效应描述性分解估计子集效应、训练随机性及剩余交互；一个 cell 只有一次运行，交互与其他残差不能进一步完全区分。报告方差分量和原始单元，不将小样本分解解释为精确总体规律。

## 6. 泛化稳健性

### 6.1 骨架分组实验

在当前数据版本上，以规范化 Murcko scaffold 分组；无环分子保留同一个显式标记的空骨架组，不为了凑比例把该组拆开。按组大小从大到小排序，同大小用 SHA-256(seed|scaffold) 破同分；每次把整组分配给距离目标人数缺口最大的 split，目标比例 80/10/10，同缺口按 train/val/test 顺序处理。种子 `20261905`。

报告实际分割比例、组数、最大组占比、空骨架比例及目标分布。使用 ID 保存划分，训练池内部按 `11、22、33` 生成嵌套序列，对应训练种子 `101、102、103`。

三条主路线、四目标、`500、5000、20000` 三规模、三重复，共 `3×4×3×3=108` 次，复用随机划分选定的架构和参数，不在测试集上重新调优。训练/输入标准化和组成等拟合仍只用相应训练子集。

若任一 split 为空或训练池不足 20k，不拆组修成想要的结果；保留不可执行诊断，并按预定替代方案做 Morgan-count Tanimoto 距离的 Butina 聚类分组，距离阈值 0.6，再按相同整组分配规则划分，明确更名为 cluster split。该替代是可行性分支，不根据模型指标选划分。

此处三个规模用于泛化稳健性，不足以单独证实完整新 scaling law；需要扩展八规模时沿用同一现成规则，可作为预先定义的扩展分析，不重选分割。

### 6.2 两个新随机划分的确认

在主实验外推结果和分析规则保存之后，增加 split seeds `20260906、20260907`，其余比例、数据版本和方法不变。每个新分割使用 subset seed 11/training seed 101，在三路线、四目标、`500、5000、50000` 上检查关键排序与局部斜率，共 72 次。

新分割会改变哪些分子属于测试集，因此这些结果单列为重新划分稳健性，不混入旧固定测试集均值，也不能回流修改主配置与主外推预测。

## 7. 结果记录、指标与成本

每次运行保存以下信息，未来执行器按此产生文件；本次文档提交不代表这些执行器已经存在。

| 产物 | 必需内容 |
|---|---|
| run manifest | 数据/特征/划分/代码提交哈希，route、target、N、两类种子、配置全文及哈希、环境、设备 |
| preprocessing | 输入 scaler、目标 scaler、组成回归参数；各自拟合使用的训练 ID 哈希 |
| learning history | 更新数/epoch/树数、训练与验证误差、学习率、最佳 checkpoint、停止原因、预算延长 |
| predictions | mol_id、split、target、unit、y_true、y_pred；train/val/test 分开保存 |
| metrics | MAE、RMSE、R²、均值/中位数基线；同时记录样本数，不能只保留四舍五入值 |
| checkpoint | 最佳模型、完整恢复状态；保存后重新加载的预测一致性检查 |
| costs | 一次性特征提取、实际训练、验证、推理耗时；峰值内存/显存、参数量、实际更新数 |
| status | completed/failed/interrupted/optimization_censored；失败理由与恢复记录 |

JSON 使用严格格式，非有限值写 null 并另给原因；预测文件保留足够浮点精度。错误指标从保存的逐分子预测独立重算，和训练器汇总交叉核对。

时间报告同时给冷启动和复用特征两种口径。MoLFormer 预训练成本若未知写“未计入/不可获得”，不写成零。硬件不同的耗时不直接作公平速度排名。没有经过核验的 FLOPs 不编造；参数量、实际训练更新数和指定设备上的实测成本分别报告。

计划运行量：主实验 480；三条表示/头对照 480；G 残差 120；容量对照 180；额外交叉种子 144；骨架 108；新随机分割 72，合计 **1,584 个新的最终配置运行单元**。其中核心主曲线始终是原有 480 次。均值/中位数/组成基线另计，Ridge 成本通常较低但仍单独记录；数字不包含 HPO、预算延长、失败重跑或第 3.1 节重新调参诊断。

重新调参诊断每个 route×target×诊断N 采用八候选/种子991、前两名再跑种子992；三条主路线为 `3×4×3×10=360` 次候选训练，胜出配置用五个主重复评价最多再增加 180 次；与已有运行完全相同则按配置哈希复用，不重复计数。其他辅助路线的同类诊断按同一公式记录，实际清单在执行前展开为表格。没有用户硬件预算，不能据此给出可信的 GPU 小时承诺。

## 8. 执行顺序与完成条件

### 阶段 A：实验准备

保留现有原始数据和特征；校验 LFS 文件不是指针、ID 顺序、数据及各输入文件哈希、目标单位、split 互斥与嵌套性。训练入口应校验实际使用的 split 文件本体，而不只校验数据版本。

以少量数据完成三条路线各一次训练、保存、重新加载、预测及指标重算。检查输入/目标 scaler 仅拟合训练 ID；检查零键分子、单批次、常数输入维度、非有限值处理和输出命名。为此需要补齐尚缺的训练/调度/评价实现，但这属于随后按本方案执行，不属于本次文档完善。

### 阶段 B：配置选择

执行固定候选表和主调参协议，保存候选结果及选定配置。数据有效性故障修复需留下版本记录；不能以某个模型暂时输掉为理由改数据。配置选择本身是方案内实验步骤，不需要重新设计研究问题。

### 阶段 C：主实验与规律开发

运行完整 480 次；首先释放 500–20k 开发结果，按第 4 节拟合和选择规律。保存可核验的外推预测及分析代码版本，然后释放 50k/100k 结果。训练可以并行，结果揭示顺序保持上述定义。

### 阶段 D：解释与稳健性

执行第 5–6 节和重新调参诊断。按预先定义规则处理可行性问题、未收敛和缺失运行，而不是看到结果后临时挑对照。辅助实验支持解释，不逐点改写主曲线。

### 阶段 E：论文与归档

从保存预测重建全部表图，检查每一实验单元是否有结果或明确失败原因。完成方法、规律验证、对照、稳健性、成本和局限章节，再整理投稿材料。

正常情况下，后续需要决定的是运行安排与实现细节，不是重新讨论目标、对照或统计方法。真实数据错误、数值实现错误或审稿新增问题仍可能要求修正；任何方案都不能保证一定发现预期规律、无需任何修正或一定被接收。出现此类问题保留旧结果、说明原因，区分确认性结果与后续探索性结果，不把历史预测覆盖掉。

## 9. 预先确定的论文图表与论证路径

| 图表 | 内容 | 支持的论点 |
|---|---|---|
| Figure 1 | 数据、三条路线、嵌套规模及外推验证流程 | 实验定义与可复现性 |
| Figure 2 | 四目标主学习曲线，全部重复/均值/误差条 | 是否存在规模规律和目标差异 |
| Figure 3 | 开发拟合、事先外推、50k/100k 观测结果 | 规律是否能预测未见训练规模 |
| Figure 4 | alpha、局部斜率、平台可辨识性、候选选择频数 | 规律形式及不确定性 |
| Figure 5 | 组成/G残差、binary/count、MLP/Ridge 对照 | 目标与表示机制 |
| Figure 6 | 容量、调参敏感性、骨架/新随机分割 | 是否为配置或划分特例 |
| Figure 7 | 数据效率与实测计算成本 | 规律的实际用途 |
| Table 1 | 数据口径、目标、输入、主及补充矩阵 | 可比较的研究边界 |
| Table 2 | 候选规律、外推误差、常数预测对照 | 定量验证而非只看拟合图 |
| Supplement | 全部超参数、逐次结果、失败、种子分解、完整残差/成本 | 完整性与复现 |

写作按证据组织：动机与相关工作 → 实验协议 → 规律发现及外推 → 机制对照 → 泛化与成本 → 局限。投稿题目随实际证据选择“scaling laws”“property-dependent scaling”或“limits of power-law extrapolation”，不为了题目好看忽略负结果。

研究贡献候选是：可外推的性质依赖规律、能区分表示/目标/容量效应的解释、或明确揭示统一幂律失效的条件。它们都需要结果支持；本方案不把尚未运行的假设写成发现。

## 10. 投稿前检查

必须能够回答：是否公开全部十二组主曲线；是否保留原始 G 和 binary 基线；50k/100k 预测是否在揭示结果前保存；超参数与候选表能否复原；标签预算和预训练来源是否准确；统计区间的条件与样本量是否清楚；测试集是否参与了某一步模型选择；结论是否区分描述拟合和外推验证；容量与优化是否足以解释平台；所有新增训练是否有代码、配置和状态记录；图表能否从预测文件重新生成；文献对比是否使用一致的数据/单位/输入；是否把尚未测量的成本和重叠写成未知。

代码与数据归档应包含环境锁定、数据及模型来源、许可检查、固定 ID 划分、配置候选、最终配置、运行清单、逐分子预测、分析脚本与失败说明。投稿期刊格式和当时政策在选定期刊后核对，不预设某个期刊一定接收。

## 11. 来源与定位

[1] 本仓库基准版本 `fdd0a611e486f25e5c4ccd8c148db9f5a6b782d2`：`README.md`、`FEATURES.md`、`config/experiment.json`、`scripts/make_splits.py`、`scripts/train_xgb.py`、`features/molformer.json`。数据及实现状态以这些文件为据；本方案新增的预算、候选、阈值和步骤是设计建议，不是已执行事实。

[2] Chen et al. (2023). *Uncovering Neural Scaling Laws in Molecular Representation Learning*. arXiv:2309.15123. https://arxiv.org/abs/2309.15123 。用于相关工作定位，不声称当前实验已经复现其结论。

[3] IBM Research. *MoLFormer-XL-both-10pct model card*. https://huggingface.co/ibm-research/MoLFormer-XL-both-10pct 。本研究使用仓库已经固定的具体模型提交，不追随模型卡默认分支更新权重。
